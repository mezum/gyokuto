# AGENTS.md

## Project Overview

- 原則 Rust (stable) を使用。
- タスク実行には [Just] を使用する。
  - 開発するために追加で必要なコマンドは [Justfile] に定義
  - 手動で実行するのは原則避ける

## Commands

- `Just lint` - コードの静的解析を行う
- `Just test` - テストコードを実行する
- `Just build` - プロジェクトをビルドする

## Restrictions

- ドキュメント類やコミットメッセージなどは日本語で書くこと
  - 他の markdown と内容が重複する場合はリンクを利用する
  - リンクは markdown の末尾にまとめ、インラインリンクは避ける
  - 必要以上に日本語に訳さず、IT 用語やカタカナ語を適宜使用すること
- 標準機能やライブラリをなるべく使用し、独自実装は最終手段とする
- 宣言的に書くことを優先し、手続き的な実装は最小限に留める
- 1 コミットの差分行数は原則最大でも 150 行程度に収める
  - どうしても内容が 150 行で完結しない時にのみ例外として 150 行以上になることを許可する
- 1 PR の差分行数は原則最大でも 250 行程度に収める
  - 超えそうな場合は PR を分割する
- clippy の警告は原則対応する、suppress は控える
- ライブラリは原則 [ルートの Cargo.toml](root-cargo.toml) に `workspace.dependency` として追加

## Boundary

- ローカル環境のに依存する設定ファイルはリポジトリに含めず、`.gitignore` に追加する
  - スコープをなるべく限定するために `.gitignore` をサブディレクトリにも置いて良い
- 外部ライブラリの改造は原則行わない

## Workflow

1. 要件を明確にして、Issue を起票
   - 言語仕様に関わる場合は、テスト作成の前に `docs/spec/` の仕様書を更新してコミット
2. 要件を満たす最小限のテストコードを作成しコミット
3. テストを通るように最小限で実装
4. テストを実行して正しく動くか確認
5. テストが失敗している場合は実装の修正と再テストを繰り返す
6. テストが全て通過したときは `Just lint` を行い必要に応じて修正
7. コミットを行い次のステップへ
8. (実行環境的に可能な場合) 別系統モデルによるレビューを依頼
   - 例: コーディングを Claude が行ったのであれば Codex にレビューを依頼
   - 別モデルによるレビューは基本 1 往復で、懸念があるときのみ 2 往復目を許可
9.  レビュー結果を受けて、必要に応じて実装を 1 つずつ修正し、対応 1 項目単位でコミット
   - 別モデルのレビュー結果は必ずしも対応すべきとは限らない
   - 修正前に致命度や頻度を考慮して実際に修正するか判断
10. レビュー対応が完了したら、 `Just lint` を行い必要に応じて修正
11. (1.) で建てた issue をクローズする PR を作成
12. Issue や PR にユーザーからのレビューコメントが付いたら、必要に応じて対応を行い、スレッドに返信する
    - スレッドはユーザーが確認の上で閉じるため、指示を受けるまでは解決済みにしないこと
13. ユーザーまたはコントリビューターの LGTM を得て、なおかつ CI が通過したら PR をマージする

## Branch name

- ブランチ名は原則 `feature/{モデル名}/issue{Issue番号}-{修正内容}`
- Issue が無い場合のみ `feature/{モデル名}/{修正内容}` とする

<!-- links -->

[Just](https://github.com/casey/just)
[Justfile](./Justfile)
[root-cargo.toml](./Cargo.toml)
